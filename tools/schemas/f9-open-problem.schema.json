{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://excelfunctionshandbook.local/schemas/f9-open-problem.schema.json",
  "title": "F9 - content/openproblems/OP-<nnn>.json",
  "description": "A promoted open problem. An open problem with no evidence record fails the build.",
  "efh": {
    "family": "F9",
    "organ": "content",
    "path_glob": "content/openproblems/OP-*.json",
    "writer": "T10 curation pass (human/agent)",
    "spec": "FOUNDATION.md 2.8 lines 372-373 ('as D1 2.7'), resolving to D1-reference-structure.md 2.7 lines 360-363",
    "propertyOrderBasis": "foundation-table",
    "keyOrderContract": "Emit keys in propertyOrder exactly; subjects[] and evidence_records[] ordinal-sorted."
  },
  "type": "object",
  "additionalProperties": false,
  "pendingRequired": {
    "schema": "FOUNDATION 2.3 line 161 requires `schema` on every file. D1 2.7's F9 field list does not name it. Recorded, not invented.",
    "record_version": "FOUNDATION 2.3 line 161 requires record_version on every authored file. D1 2.7's F9 field list does not name it. Recorded, not invented."
  },
  "propertyOrder": [
    "open_problem_id",
    "title",
    "subjects",
    "evidence_records",
    "statement",
    "why_it_matters",
    "what_would_settle_it",
    "status",
    "settled_by",
    "episode"
  ],
  "required": [
    "open_problem_id",
    "title",
    "subjects",
    "evidence_records",
    "statement",
    "why_it_matters",
    "what_would_settle_it",
    "status"
  ],
  "properties": {
    "open_problem_id": { "type": "string", "pattern": "^OP-[0-9]{3}$", "description": "R." },
    "title": { "type": "string", "description": "R." },
    "subjects": { "type": "array", "minItems": 1, "items": { "type": "string" }, "description": "R." },
    "evidence_records": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "pattern": "^EV-[a-z-]+-[0-9]{4}$" },
      "description": "R, >=1. An open problem with no evidence record FAILS THE BUILD (D1 2.7 line 361; T10 acceptance test (d), FOUNDATION line 1190)."
    },
    "statement": { "type": "string", "description": "R." },
    "why_it_matters": { "type": "string", "description": "R." },
    "what_would_settle_it": { "type": "string", "description": "R." },
    "status": {
      "type": "string",
      "enum": ["open", "narrowed", "settled", "withdrawn"],
      "description": "R."
    },
    "settled_by": { "type": "string", "description": "C. Required iff status == settled." },
    "episode": { "type": "integer", "description": "O. Int into F10." }
  }
}
